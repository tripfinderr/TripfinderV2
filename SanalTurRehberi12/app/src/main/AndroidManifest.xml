<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.crinsoft.sanalturrehberi">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".YorumlariGor"></activity>
        <activity android:name=".EnCokTercihEdilenTurlar" />
        <activity android:name=".GittigimTurlar" />
        <activity android:name=".NotlarBaslikliGoster" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".GeneralGoogleMaps"
            android:label="@string/title_activity_general_google_maps" />
        <activity android:name=".FavlariGoster" />
        <activity android:name=".HavaDurumu" />
        <activity android:name=".SSSorular" />
        <activity android:name=".Notlarim" />
        <activity android:name=".SifreUnuttum" />
        <activity android:name=".YorumYapActivity" />
        <activity android:name=".MekanDetaylariActivity" />
        <activity android:name=".TurMekanlari" />
        <activity android:name=".SurnameUpdate" />
        <activity android:name=".NameUpdate" />
        <activity android:name=".SeyehatPlanlari" />
        <activity
            android:name=".GoogleMapActivity"
            android:label="@string/title_activity_google_map" />
        <activity android:name=".HesapBilgileri" />
        <activity android:name=".FeedSayfasi" />
        <activity android:name=".KayitOlActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>